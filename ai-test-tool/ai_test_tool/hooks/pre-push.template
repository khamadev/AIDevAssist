#!/usr/bin/env bash
# Installed by ai-test-tool's `init` command. Do not edit directly —
# edit ai_test_tool/hooks/pre-push.template instead and re-run init.
set -e
cd "$(git rev-parse --show-toplevel)"
PYTHONPATH="ai-test-tool" python -m ai_test_tool.cli orchestrate --stage pre-push --target .
